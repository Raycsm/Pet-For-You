import { StyleSheet } from "react-native";
import * as React from "react";

const theme = {
    colors: {
        primary: '#DB652F',
        accent: '#DB652F',
        // secondary: "#db652f",
		// terticiary: "#db652f",
    }
};

export default theme;