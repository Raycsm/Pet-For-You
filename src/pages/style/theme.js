import { StyleSheet } from "react-native";
import * as React from "react";

const theme = {
    colors: {
        accent: '#DB652F',
        primary: "#db652f",
        secondary: "#db652f",
        terticiary: "#db652f",
    }
};

export default theme;